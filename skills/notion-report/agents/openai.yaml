interface:
  display_name: "Notion Report"
  short_description: "Create and file reports directly in Notion"
  default_prompt: "Create a report directly in Notion using the local notion-local MCP API tools, filed under the appropriate project hub. Preserve detail, include tables, and strive relentlessly to embed plots/images using valid external image blocks. Avoid leaking local paths, and return the created page URL."
