interface:
  display_name: "Notion Report"
  short_description: "Create and file reports directly in Notion"
  default_prompt: "Create a report directly in Notion (via the Notion MCP tools), filed under the appropriate project hub. Preserve detail, include tables, and strive relentlessly to embed plots/images (try every available approach supported by Notion-flavored Markdown). Avoid leaking local paths, and return the created page URL."
